[project]
name = "commcare-sync"
version = "0.1.0"
description = "CommCare Sync simplifies the management of your CommCare data pipeline"
readme = "README.md"
requires-python = "~=3.10.0"  # TODO: ">=3.10,<3.15"
dependencies = [
    "Django~=5.2",
    "django-allauth",
    "psycopg2-binary",
    "django-anymail[mailgun]",  # email services
    "celery-progress",
    "celery[redis]",
    "django-reversion>=5.0.4",
    "commcare-export==1.13.0",
    "pyopenssl>=20.0.1",  # here to force upgrades of commcare-export dependencies
]

[project.optional-dependencies]
prod = [
    "gunicorn",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    {include-group = "lint"},
    "pip-tools",
    "fabric",
]
lint = [
    "ruff",
]

[tool.uv.sources]
